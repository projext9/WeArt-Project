<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "we.are.travelers.test">

	<select id="testlogin" resultType="map">
		select * from weart_member where member_id=#{member_id} and member_pwd=#{member_pwd} and member_delyn='N'
	</select>
	
	<select id="testlogincompany" resultType="map">
		select * from weart_company where company_id=#{member_id} and company_pwd=#{member_pwd} and company_delyn='N'
	</select>

</mapper>
